{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "npx nx run twenty-server:build --skip-nx-cache"
  },
  "deploy": {
    "startCommand": "NODE_ENV=production npx nx run twenty-server:start",
    "healthcheck": false
  }
}
